import 'package:flutter/material.dart';

/// 分类页（占位版）
class CategoryPage extends StatelessWidget {
  const CategoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('分类（待实现）')),
    );
  }
}