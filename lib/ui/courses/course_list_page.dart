import 'package:flutter/material.dart';

/// 全部课程页（占位版）
class CourseListPage extends StatelessWidget {
  const CourseListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('课程（待实现）')),
    );
  }
}