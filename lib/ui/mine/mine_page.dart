import 'package:flutter/material.dart';

/// 我的页（占位版）
class MinePage extends StatelessWidget {
  const MinePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('我的（待实现）')),
    );
  }
}